PORT=5000
JWT_SECRET=your_jwt_secret_key_here
NODE_ENV=development

# Note: This project now uses SQLite (file-based database)
# No need to configure a database URL - SQLite creates database.sqlite automatically
